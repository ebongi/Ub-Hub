/// Named corner-radius scale used across the redesigned screens.
class AppRadius {
  AppRadius._();

  /// Pill/stadium shape for chips.
  static const double chip = 999.0;

  /// Standard content card radius (grid cards, carousel cards, list rows).
  static const double card = 16.0;

  /// Dialogs and bottom sheets.
  static const double sheet = 24.0;
}
