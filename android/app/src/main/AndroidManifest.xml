<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Foreground-service downloads for the on-device AI model (~530MB):
         without these, a large download on a slow/interrupted connection
         is subject to WorkManager's ~9-minute background-task kill. See
         lib/services/gemma_model_manager.dart's downloadModel(). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>

    <!-- Android 13+ requires this to be declared for notifications to be
         grantable at runtime. firebase_messaging / flutter_local_notifications
         trigger the actual prompt (see NotificationService). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <queries>
        <!-- If your app opens https URLs -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <!-- If your app opens email -->
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
        <!-- If your app opens phone numbers -->
        <intent>
            <action android:name="android.intent.action.DIAL" />
            <data android:scheme="tel" />
        </intent>
    </queries>

    <application
        android:label="GoStudy"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Safety net: if an FCM message ever arrives carrying a `notification`
             block, the OS renders it on this channel (created in
             NotificationService._createAndroidChannels). The Edge Function
             currently sends data-only messages, which the app renders itself. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="go_study_alerts" />

        <!-- WorkManager's own SystemForegroundService (used by
             background_downloader's foreground: true downloads, see
             gemma_model_manager.dart) ships with no
             android:foregroundServiceType set on its <service> element —
             WorkManager can't know in advance what type a consuming app's
             foreground work needs. On API 34+ this throws a hard
             IllegalArgumentException at runtime ("foregroundServiceType ...
             is not a subset of ... 0x00000000") unless the app declares it
             explicitly here, merging into/overriding WorkManager's library
             manifest entry. Confirmed via a real on-device crash trace this
             session (FATAL EXCEPTION on SystemForegroundService.startForeground). -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
